# SCA scan by Aikido under SAST scan
name: SCA Workflow

on:
  pull_request:


jobs:
  sca:
    runs-on: ubuntu-latest

    steps:
      - name: SVA scan
        shell: bash
        run: |
          echo "SCA scan done using Aikido configured under SAST scan."